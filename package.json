{
  "name": "stadium-lights",
  "version": "2.0.0",
  "description": "Synchronized phone display for stadiums and equestrian events",
  "type": "module",
  "main": "src/server/index.js",
  "scripts": {
    "build": "node scripts/build.js",
    "test": "node --test",
    "prestart": "npm run build",
    "start": "node src/server/index.js",
    "dev": "npm run build && node src/server/index.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "nosleep.js": "^0.12.0",
    "qrcode": "^1.5.4",
    "socket.io": "^4.7.4",
    "web-haptics": "^0.0.6"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
